# 9. Identity

Every Entity MUST define an ID.

IDs MUST be unique.

IDs SHOULD NOT be reused.

Format: `PREFIX-NAME`

Examples:
- `COMP-AUTH`
- `SCN-LOGIN`
- `RES-USERS`
- `TEST-LOGIN`
- `PRINCIPAL-USER`
